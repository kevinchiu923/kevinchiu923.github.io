<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= page.title %> - <%= config.title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="<%= config.root %>css/style.css">
</head>

<body class="dark">
    <main class="container">
        <h2 class="post-title">About</h2>
        <% if (page.about && page.about.length) { %>
            <div class="about-cards">
                <% page.about.forEach(about=> { %>
                    <div class="about-card">
                        <img class="about-avatar" src="<%= about.avatar %>" alt="<%= about.name %>'s avatar">
                        <div class="about-name">
                            <%= about.name %>
                        </div>
                        <div class="about-desc">
                            <%= about.description %>
                        </div>
                        <a class="about-link" href="<%= about.link %>" target="_blank">Visit</a>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <p>No about added yet!</p>
                <% } %>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %>
                    <%= config.author %>. Powered by <a href="https://hexo.io">Hexo</a>.</p>
        </div>
    </footer>

    <style>
        body {
            /* background: #000; */
            /* color: #fff; */
            /* font-family: sans-serif; */
        }

        .navbar {
            background: #111;
            padding: 1rem 0;
        }

        .site-title {
            /* color: #fff; */
            text-decoration: none;
            font-weight: bold;
            font-size: 1.4rem;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .post-title {
            margin-top: 2rem;
            font-size: 2rem;
            font-weight: bold;
        }

        .about-cards {
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            margin-top: 2rem;
            justify-content: center;
        }

        .about-card {
            /* background: #1e1e1e; */
            border-radius: 16px;
            padding: 1rem;
            width: 220px;
            text-align: center;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        .about-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }

        body[data-theme='dark'] .about-name,
        body[data-theme='dark'] .about-desc {
            color: #fff;
        }

        body[data-theme='light'] .about-name,
        body[data-theme='light'] .about-desc {
            color: #000;
        }

        .about-name {
            margin-top: 0.8rem;
            font-weight: bold;
            font-size: 1.1rem;
        }

        .about-desc {
            font-size: 0.9rem;
            margin: 0.5rem 0;
        }

        .about-link {
            background: transparent;
            border: 2px solid #007aff;
            color: #007aff;
            padding: 0.4rem 0.8rem;
            border-radius: 8px;
            text-decoration: none;
            display: inline-block;
            font-size: 0.9rem;
            font-weight: bold;
        }

        .about-link:hover {
            text-decoration: none;
        }

        .footer {
            /* background: #111; */
            padding: 1rem 0;
            text-align: center;
            margin-top: 2rem;
            font-size: 0.9rem;
        }
    </style>
</body>

</html>
