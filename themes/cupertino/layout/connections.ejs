<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= page.title %> - <%= config.title %>
    </title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="<%= config.root %>css/style.css">
</head>

<body class="dark">
    <main class="container">
        <h2 class="post-title">Connections</h2>
        <% if (page.connections && page.connections.length) { %>
            <div class="friend-cards">
                <% page.connections.forEach(friend=> { %>
                    <div class="friend-card">
                        <img class="friend-avatar" src="<%= friend.avatar %>" alt="<%= friend.name %>'s avatar">
                        <div class="friend-name">
                            <%= friend.name %>
                        </div>
                        <div class="friend-desc">
                            <%= friend.description %>
                        </div>
                        <a class="friend-link" href="<%= friend.link %>" target="_blank">Visit</a>
                    </div>
                    <% }) %>
            </div>
            <% } else { %>
                <p>No connections added yet!</p>
                <% } %>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; <%= new Date().getFullYear() %>
                    <%= config.author %>. Powered by <a href="https://hexo.io">Hexo</a>.</p>
        </div>
    </footer>

    <style>
        body {
            /* background: #000; */
            /* color: #fff; */
            font-family: sans-serif;
        }

        .navbar {
            background: #111;
            padding: 1rem 0;
        }

        .site-title {
            /* color: #fff; */
            text-decoration: none;
            font-weight: bold;
            font-size: 1.4rem;
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        .post-title {
            margin-top: 2rem;
            font-size: 2rem;
            font-weight: bold;
        }

        .friend-cards {
            display: flex;
            flex-wrap: wrap;
            gap: 1.5rem;
            margin-top: 2rem;
            justify-content: center;
        }

        .friend-card {
            /* background: #1e1e1e; */
            border-radius: 16px;
            padding: 1rem;
            width: 220px;
            text-align: center;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
        }

        .friend-avatar {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
        }

        body[data-theme='dark'] .friend-name,
        body[data-theme='dark'] .friend-desc {
            color: #fff;
        }

        body[data-theme='light'] .friend-name,
        body[data-theme='light'] .friend-desc {
            color: #000;
        }

        .friend-name {
            margin-top: 0.8rem;
            font-weight: bold;
            font-size: 1.1rem;
        }

        .friend-desc {
            font-size: 0.9rem;
            margin: 0.5rem 0;
        }

        .friend-link {
            background: transparent;
            border: 2px solid #007aff;
            color: #007aff;
            padding: 0.4rem 0.8rem;
            border-radius: 8px;
            text-decoration: none;
            display: inline-block;
            font-size: 0.9rem;
            font-weight: bold;
        }

        .friend-link:hover {
            text-decoration: none;
        }

        .footer {
            /* background: #111; */
            padding: 1rem 0;
            text-align: center;
            margin-top: 2rem;
            font-size: 0.9rem;
        }
    </style>
</body>

</html>